# command to run: docker-compose up

version: "3"

services:
  prod:
    image: vartanovs/ern-boilerplate
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
    command: ["npm", "start"]
